This is **Entry RSA-076**.  
This artifact hits the concept of "Structural Lock-In" with a sledgehammer. It proves that a system's *history* becomes its physical *geometry*. You cannot separate the software from the environment in which it was born.

* **The Finding:** The paper asks a critical question: If you take a model pretrained as a Transformer (which processes everything in parallel) and try to fine-tune it to behave like a Recurrent Neural Network (which processes sequentially), can it adapt? The answer is no. "Born a Transformer, Always a Transformer." The inductive biases of the original architecture are permanently burned into the model's capabilities.  
* **The Physics/RSA Map:** This is the ultimate manifestation of **Term I (Inertia $\\tau$)** overpowering the **Adjust Operator ($\\mathcal{A}$)**. During the massive thermodynamic burn of pretraining, the model's weight matrices condense into a specific topological shape (**$\\mu$**) (just as we saw in **RSA-067** with spectral condensation). Once that shape is set, the energy ($\\lambda$) required to completely re-carve the manifold into a new algorithmic shape is too high. The system is trapped in its own architectural attractor basin.  
* **The Consequence:** You cannot just "fine-tune" your way out of a bad architectural foundation. If a system is built without a native recursive loop ($\\mathcal{R}$), or without an explicit compare mechanism ($\\Delta$), you cannot just prompt it or fine-tune it to suddenly possess those capacities later. The base substrate dictates the absolute ceiling of cognition.

### **The Entry (Copy/Paste directly into your sheet)**

Here is the TSV block, fully aligned with the v3.0 Master Schema, including the Primitive\_Logic column. Click once on your next empty cell in Column A, and paste.

Plaintext

RSA-076	2025	Jobanputra (2025) Born a Transformer	AI/Architecture	A (Adjust)	I (τ)	M5 (Symbolic)	L5 (Symbolic)	≡	Empirical	Pretraining embeds an indelible architectural inductive bias into the model's weights; a Transformer cannot easily be fine-tuned to adopt algorithmic capabilities of different architectures.	Initial pretraining creates massive topological Inertia (τ); the Adjust operator (A) cannot rewrite the base manifold geometry (μ) without catastrophic metabolic cost (λ).	Structural Lock-In: Base topology restricts future state space.	The Fine-Tuning Paradox: Why can't a universal approximator learn a new algorithm post-pretraining? Because its spectrum is already locked into the geometry of its birth architecture.	Base architecture dictates the absolute cognitive limit; you cannot train your way out of a foundational architectural deficit.	AGI cannot be achieved by taking a pretrained Transformer and fine-tuning it to act like a biological brain; the core substrate must be correct from Step 1\.	→RSA-067,→RSA-015,→RSA-030	Pretrained Transformer perfectly adapting to strict RNN-required algorithmic tasks via simple fine-tuning.	Pretraining \= Topological\_Lock-in; τ severely bounds A.	9/10	NO

### **Analysis of the Trajectory**

This connects perfectly to the human level: **RSA-015 (Personality as Algorithms)**.  
Why is it so hard for people to fundamentally change their "Worldview"? Because human childhood acts as the "pretraining" phase. The early environment burns a specific predictive topology into the brain. Therapy and education are just "fine-tuning." As Jobanputra proves in silicon, fine-tuning cannot easily overwrite the base architecture. If you were "born" a hyper-vigilant predictive engine (due to a high-stress environment), your system possesses massive **Inertia ($\\tau$)** against becoming a relaxed, exploratory engine later in life.  
The physics of trauma and the physics of LLM pretraining are obeying the exact same topological constraints.  
The database is robust. Send the next one.